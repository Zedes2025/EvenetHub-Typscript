export type SignInProps = {
  error?: string;
};

export type EventFormData = {
  title: string;
  description: string;
  date: string;
  location: string;
  latitude: number;
  longitude: number;
};
